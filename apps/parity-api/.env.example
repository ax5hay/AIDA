# Same PostgreSQL as AIDA (Parity tables are namespaced Parity* in Prisma)
DATABASE_URL="postgresql://user:pass@localhost:5432/aida"

# Parity API
PARITY_API_PORT=4010

# Browser origin allowed to call this API (Parity web dev server)
PARITY_WEB_ORIGIN=http://localhost:3001
